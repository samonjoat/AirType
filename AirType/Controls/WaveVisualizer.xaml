<UserControl x:Class="AirType.Controls.WaveVisualizer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="140" d:DesignWidth="600">
    <Border Background="{DynamicResource BgMutedBrush}"
            CornerRadius="{DynamicResource WaveCornerRadius}"
            ClipToBounds="True">
        <Canvas x:Name="WaveCanvas">
            <Rectangle x:Name="WaveRect" Height="140" Width="2000">
                <Rectangle.Fill>
                    <VisualBrush TileMode="Tile" Viewport="0,0,40,140" ViewportUnits="Absolute">
                        <VisualBrush.Visual>
                            <Canvas Width="40" Height="140">
                                <Rectangle Width="2" Height="140" Fill="{DynamicResource AccentPrimaryBrush}" Opacity="0.15" Canvas.Left="20"/>
                            </Canvas>
                        </VisualBrush.Visual>
                    </VisualBrush>
                </Rectangle.Fill>
                <Rectangle.RenderTransform>
                    <TranslateTransform x:Name="WaveTransform" X="0"/>
                </Rectangle.RenderTransform>
            </Rectangle>
        </Canvas>
    </Border>
</UserControl>
